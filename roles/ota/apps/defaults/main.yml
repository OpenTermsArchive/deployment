ota_apps_read_config_only: false
ota_apps_repository_branch: main
ota_apps_directory: "{{ ota_apps_repository | urlsplit('path') | regex_replace('^.*/(.+?)(\\.git)?$', '\\1') }}" # extract the repository name from the URL
